'use strict';

var immutable = require('immutable');
var reselect = require('reselect');

const createImmutableSelector = reselect.createSelectorCreator(reselect.defaultMemoize, immutable.is);

module.exports = createImmutableSelector;
