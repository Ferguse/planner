import styled from 'styled-components';

export default styled.div`
  position: relative;
  width: 80%;
  
  p {
    padding: 0;
    margin: 0;
  }
  
  p:first-child {
    text-transform: uppercase;
    font-weight: bold;
  }
`;
